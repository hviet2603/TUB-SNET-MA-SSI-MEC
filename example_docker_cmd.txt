# Build

docker build -t vdocker2603/ma-ssi-apps:latest -f infra/deployment/dockerfiles/apps.dockerfile .

# Run

docker run -p 3000:3000 -p 3001:3001 -p 3002:3002 \
    -e AGENT_HOST=192.168.0.1 \
    vdocker2603/ma-ssi-apps uvicorn app.app_authorities.main:app --host 0.0.0.0 --port 3002
